/**
 * Central place for server identity and runtime configuration.
 * Keep this small and import it wherever the name/version is needed.
 */
export const APP_NAME = "mcp-forge";
export const APP_VERSION = "1.0.0";
